Task: Dockerfile fix + .dockerignore cleanup
Plan: skillforge-optimization / checkbox 1
Date: 2026-05-06

## Changes Made

### Dockerfile
- Removed orphan `deps` stage (lines 1-4: `FROM node:20-alpine AS deps` ... `RUN npm ci --only=production`)
- Builder stage now starts at line 1: `FROM node:20-alpine AS builder`
- No changes to builder or runner stage logic
- File reduced from 30 to 26 lines (25 content + 1 trailing)

### .dockerignore
- Removed duplicate `.next` entry (was on both line 4 and line 7)
- Added missing entries:
  - `stitch-designs/` (after node_modules)
  - `examples/` (after stitch-designs/)
  - `.claude/` (after examples/)
- File increased from 11 to 13 lines

## Verification

### Passed (Static Checks)
- [x] `Select-String "AS deps" Dockerfile` → no output (deps stage removed)
- [x] `Select-String "\.next" .dockerignore` → exactly 1 match at line 7
- [x] Dockerfile reads clean: starts with `FROM node:20-alpine AS builder`
- [x] .dockerignore entries added in correct positions

### Skipped (Environment Limitation)
- [ ] `docker build -t skillforge-test .` — Docker CLI not available in dev environment
- [ ] `npm run build` — standalone build verification

## Files Modified
- F:\claude-migration\skillforge\Dockerfile
- F:\claude-migration\skillforge\.dockerignore
